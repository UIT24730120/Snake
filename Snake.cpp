#include <iostream>
#include <windows.h> // Cho gotoxy, Sleep, COORD, GetStdHandle, SetConsoleCursorPosition
#include <cstdlib>   // Cho rand, srand, system
#include <conio.h>   // Cho kbhit, getch
#include <ctime>     // Cho time (để khởi tạo bộ sinh số ngẫu nhiên)
#include <vector>    // Sử dụng vector thay cho mảng cố định sẽ linh hoạt hơn, nhưng ta sẽ giữ nguyên
